name: DX bootstrap (temporary)
on:
  push:
    branches: [dx/tooling-bootstrap]
permissions:
  contents: read
concurrency:
  group: dx-bootstrap
  cancel-in-progress: true
jobs:
  prepare:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          persist-credentials: false
          fetch-depth: 0
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: '3.13'
      - name: Collect exact source and public dependency wheels
        run: |
          mkdir -p dx-bootstrap/wheels
          git bundle create dx-bootstrap/source.bundle HEAD
          python -m pip download --only-binary=:all: --dest dx-bootstrap/wheels ruff pyrefly complexipy tach hypothesis pre-commit pytest pytest-cov uv hatchling build types-pyserial scipy-stubs
          python -m pip download --only-binary=:all: --dest dx-bootstrap/wheels -r requirements-tested.txt -r requirements-serial.txt
          sha256sum dx-bootstrap/source.bundle dx-bootstrap/wheels/* > dx-bootstrap/SHA256SUMS.txt
          python --version
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: dx-bootstrap
          path: dx-bootstrap/
          if-no-files-found: error
          retention-days: 1
          compression-level: 0
